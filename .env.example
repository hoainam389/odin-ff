# Supabase (auto-injected by Vercel Marketplace integration in prod)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Postgres connection (Supabase pooler 6543 for app, 5432 for migrations)
DATABASE_URL=
DIRECT_URL=

# Admin auth
ADMIN_USERNAME=admin
ADMIN_PASSWORD=niteco123
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=
